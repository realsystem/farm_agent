name: "Farm Agent"
version: "0.3.2"
slug: "farm_agent"
description: "AI agent for remote farm monitoring"
init: false

arch:
  - aarch64
  - amd64

homeassistant_api: true

options:
  openai_api_key: ""

schema:
  openai_api_key: password

ports:
  8080/tcp: 8080
